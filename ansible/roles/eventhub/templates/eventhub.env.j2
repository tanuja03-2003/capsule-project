# Managed by Ansible for the {{ application_name }} host configuration.
# Secrets are intentionally excluded. Store secret values with Ansible Vault.
EVENTHUB_APPLICATION_NAME={{ application_name }}
EVENTHUB_ENVIRONMENT={{ environment_name }}
EVENTHUB_PORT={{ application_port }}
EVENTHUB_BACKEND_IMAGE={{ docker_registry }}/{{ backend_image }}:{{ backend_image_tag }}
EVENTHUB_NOTIFICATION_WORKER_IMAGE={{ docker_registry }}/{{ notification_worker_image }}:{{ notification_worker_image_tag }}
